# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: remote-mcp-dotnet-oauth
metadata:
  template: remote-mcp-dotnet-oauth@0.0.1-beta
services:
  web:
    project: src/
    language: dotnet
    host: appservice